/* Compiles the portable controller (tools/stm32_follow_app/follow_controller.c)
 * as part of the app, so the firmware build and the host tests share one source. */
#include "../../follow_controller.c"
